soon to test compatibility with megaraid cards, need to dev a solution after testing if the library being used isn't compatible.

probably scan and run commands from regular bash, parse standard output.
